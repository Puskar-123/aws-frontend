import React,{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import{chatRequest}from"../../services/chatApi";const RepositoryChatPage=()=>{const{id}=useParams(),navigate=useNavigate(),[error,setError]=useState("");useEffect(()=>{chatRequest(`/repo/${id}/chat`).then(data=>navigate(`/chat?conversation=${data.conversation._id}`,{replace:true})).catch(e=>setError(e.message));},[id,navigate]);return <main role={error?"alert":"status"}>{error||"Opening repository chat…"}</main>};export default RepositoryChatPage;
