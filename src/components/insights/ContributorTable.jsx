import React from "react";
const metric = (value) => value == null ? "Unavailable" : Number(value).toLocaleString();
const ContributorTable = ({ data }) => <section className="insights-panel"><h2>Contributors</h2>{!data?.contributors?.length ? <p className="insights-empty">No contributor data is available yet.</p> : <div className="insights-table-scroll"><table><thead><tr><th scope="col">Contributor</th><th scope="col">Commits</th><th scope="col">Additions</th><th scope="col">Deletions</th><th scope="col">Files changed</th><th scope="col">Last contribution</th></tr></thead><tbody>{data.contributors.map((item) => <tr key={`${item.user?._id || item.name}`}><th scope="row">{item.user?.username || item.name || "Deleted contributor"}</th><td>{metric(item.commits)}</td><td>{metric(item.additions)}</td><td>{metric(item.deletions)}</td><td>{metric(item.filesChanged)}</td><td>{item.lastContributionAt ? <time dateTime={item.lastContributionAt}>{new Date(item.lastContributionAt).toLocaleDateString()}</time> : "Unknown"}</td></tr>)}</tbody></table></div>}</section>;
export default ContributorTable;
