import React from "react";
const BranchSummary = ({ data }) => <section className="insights-panel"><h2>Branch activity</h2>{!data?.branches?.length ? <p className="insights-empty">No branches are available.</p> : <div className="insights-table-scroll"><table><thead><tr><th scope="col">Branch</th><th scope="col">Commits</th><th scope="col">Last activity</th></tr></thead><tbody>{data.branches.map((branch) => <tr key={branch.name}><th scope="row">{branch.name} {branch.isDefault && <span className="insights-label">Default</span>} {branch.protected && <span className="insights-label">Protected</span>}</th><td>{branch.commits}</td><td>{branch.lastCommitAt ? <time dateTime={branch.lastCommitAt}>{new Date(branch.lastCommitAt).toLocaleDateString()}</time> : "No activity"}</td></tr>)}</tbody></table></div>}</section>;
export default BranchSummary;
